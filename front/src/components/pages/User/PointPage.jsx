import { useNavigate } from "react-router-dom";
import "../../../styles/MyPage.css";

const PointPage = () => {
  const navigate = useNavigate();

  return <div>포인트 이력페이지</div>;
};

export default PointPage;
